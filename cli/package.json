{
  "name": "web-finder",
  "version": "1.3.9",
  "description": "Find web interfaces published by WebFinder clients on your Tailscale devices",
  "bin": {
    "web-finder": "./bin/web-finder"
  },
  "scripts": {
    "lint": "eslint scanner.js bin/web-finder",
    "check": "npm run lint && node --check scanner.js && node --check bin/web-finder"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.4.0",
    "globals": "^17.6.0"
  }
}
